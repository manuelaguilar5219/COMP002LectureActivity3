function greet(name) { 
    return "Hello, " + name + "!"; 
} 
// Function that outputs a greeting
function getCircumference(diameter) {
     const pi = 3.14159;  
}
// Function that gives the approximate circumference of a circle given the diameter
function firstFunction() {
     console.log("Inside firstFunction");
     
     secondFunction(); 
     
     console.log("Back in firstFunction after calling secondFunction"); 
} 
    function secondFunction() {
     console.log("Inside secondFunction");     
} // it goes from first to the last function